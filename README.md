# Mission Control

Next.js dashboard for OpenClaw operations.

## Backend setup

See [BACKEND.md](./BACKEND.md).

Short version:

```bash
cp .env.example .env.local
npm install
npm run dev
```

Default backend mode is `demo` (no credentials required).

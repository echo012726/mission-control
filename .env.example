# Backend mode:
# - demo (default, no credentials, safe fallback)
# - openclaw (reads local OpenClaw runtime files)
# - convex (reads Convex via HTTP API)
MISSION_CONTROL_BACKEND=demo

# Convex (only needed when MISSION_CONTROL_BACKEND=convex)
NEXT_PUBLIC_CONVEX_URL=
CONVEX_ADMIN_TOKEN=

# Optional OpenClaw path overrides (only for MISSION_CONTROL_BACKEND=openclaw)
# OPENCLAW_MEMORY_DIR=/root/.openclaw/workspace/memory
# OPENCLAW_SESSIONS_FILE=/root/.openclaw/agents/main/sessions/sessions.json
# OPENCLAW_MAILDIR_INBOX=/root/Maildir/INBOX
